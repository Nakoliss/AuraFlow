{
  "name": "@aura-flow/cron",
  "version": "1.0.0",
  "private": true,
  "description": "AuraFlow scheduled jobs with Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'Wrangler dev not supported in WebContainer. Use local environment for Cloudflare Workers development.'",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "vitest --run",
    "test:watch": "vitest",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@aura-flow/common": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230914.0",
    "typescript": "^5.2.0",
    "vitest": "^0.34.0",
    "wrangler": "^3.10.0"
  }
}